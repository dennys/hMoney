
7
Were__ you and your friends at the movies yesterday ?
No, we weren't___. We were____ at a baseball game.
Was____ the game interesting?
No, it wasn't_____. It was_____ boring.

Was Tim at home last night?
No, he wasn't________ he is at the _____?????_____you haven' tell me the question so I can't answer please read the question carefully
Were your brothers awake at 6am this morning?
No, we weren't________ we were alseep ?????__________you haven' tell me the question so I can't answer please read the question carefully

Correct:
Sorry I wasn't at the basketball game on Tuesday afternoon.
I was sick at home. It was really boasnWas

====================================================================
6
I live in____ Denver. Denver is a big city in____ Colorado. 
My apartment is at____ 143 Oak Stree. It's bewteen _________ 141 Oak Street and 149 Oak Street.
I live on the_____ fourth floor

My school is _at__ 695 Part Avenue. My class is _in___ Room 1502.
It's _on___ the 15th floow. My school is __across__ from a samll part. It's ___next to__ a supermarket.

Correct:
A: Is your apartment in the second floor?
B: No, it's on the third floor.
A: Where's the bank?
B: It's on the corner of First Avenue. It's bewten  8th and 9th Streets.  your bewteen is spelled wrong please fixed
A: Is it next to the post office?
B: Yes, it is.

THANK YOU FOR ALL OF YOUR WORK THANKS YOU ARE NICE AND HELPFUL  
YOU BRAIN IS SO GOOD AND YOU CAN MAKE QUESTION VERY GOOD!

714199
SONAR_TOKEN
name: Build
on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: Build
    runs-on: windows-latest
    steps:
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 1.11
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Cache SonarCloud packages
        uses: actions/cache@v1
        with:
          path: ~\sonar\cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache SonarCloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@v1
        with:
          path: .\.sonar\scanner
          key: ${{ runner.os }}-sonar-scanner
          restore-keys: ${{ runner.os }}-sonar-scanner
      - name: Install SonarCloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          New-Item -Path .\.sonar\scanner -ItemType Directory
          dotnet tool update dotnet-sonarscanner --tool-path .\.sonar\scanner
      - name: Build and analyze
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        shell: powershell
        run: |
          .\.sonar\scanner\dotnet-sonarscanner begin /k:"dennys_hMoney" /o:"dennys" `
            /d:sonar.login="c70a53862de980131b1c6071a6123ae11e259f6d" `
            /d:sonar.host.url="https://sonarcloud.io"
          #dotnet build hMoney.sln
          dotnet test --settings coverlet.runsettings --logger:trx hMoney.sln
          #dotnet test hMoney.sln
          #dotnet test hMoney.sln collect:"XPlat Code Coverage" `
          #  --results-directory TestResults/ `
          #  --logger "trx;LogFileName=unittests.trx" `
          #  --no-build --no-restore --configuration release `
          #  --DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=opencover
          #dotnet test ".\hMoneyVSTests\hMoneyVSTests.csproj" collect:"XPlat Code Coverage" --results-directory TestResults/ --logger "trx;LogFileName=unittests.trx" --no-build --no-restore --configuration release --DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=opencover

          #dotnet build hMoney\hMoney.csproj
          #dotnet build test hMoney\hMoney.csproj
          #dotnet build test hMoneyTests\hMoneyTests.csproj
          .\.sonar\scanner\dotnet-sonarscanner end /d:sonar.login="c70a53862de980131b1c6071a6123ae11e259f6d"
